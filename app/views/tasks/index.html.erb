<div class="nav_active" id="all_tasks">
  <ul>
    <% @tasks.each do |task| %>
    <li><%= link_to( task.name, task ) %> | <%= link_to( "Edit", edit_task_path( task ) ) %></li>
    <% end %>
  </ul>
</div>
